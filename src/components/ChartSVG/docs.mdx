import { Story, Canvas } from "@storybook/addon-docs/blocks";

# ChartSVG

Returns an SVG element into which the chart elements can be rendered.

<Canvas>
  <Story id="chartsvg--default" />
</Canvas>
